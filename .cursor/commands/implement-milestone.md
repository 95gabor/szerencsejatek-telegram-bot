You are working on the Szerencsejáték Telegram bot (Deno + TypeScript).

1. Read `docs/design-plan.md` and identify the current milestone.
2. Read `docs/architecture.md` and `docs/requirements.md` for constraints.
3. Implement only what that milestone needs; avoid scope creep.
4. Add or update tests (`deno test`) for new domain logic.
5. **Review** the code: run `deno task check`, re-read the diff for scope and naming, remove noise.
6. Summarize changes as a short bullet list: files touched, how to run tests, any new env vars.

If a requirement is ambiguous, stop and list **concrete questions** for the user before coding.
